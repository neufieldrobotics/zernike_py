before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - test

test:python35:
  image: python:3.5
  script:
  - python -m unittest discover -s tests -v

test:python36:
  image: python:3.6
  script:
  - python -m unittest discover -s tests -v

test:python37:
  image: python:3.7
  script:
  - python -m unittest discover -s tests -v
